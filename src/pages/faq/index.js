import React from "react";

import Layout from "../../components/Layout";

import {
    MainSection
} from "../../sections/FaqPage";

const FAQ = ({ path }) => (
    <Layout path={path}>
        <MainSection />        
    </Layout>
);

export default FAQ;
